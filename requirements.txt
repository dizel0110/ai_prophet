# Core
aiogram
google-genai
python-dotenv

# API & DNS
fastapi
uvicorn
python-multipart
dnspython
requests
huggingface-hub

# Proxy support (для HF Spaces)
aiohttp-socks

# Search
duckduckgo-search

# Media (требует ffmpeg в системе)
yt-dlp

# Database (опционально, для Supabase)
psycopg2-binary

# ─── Опционально (не влезает на HF Spaces free) ───
# Локальная транскрибация Whisper. ~2GB с torch.
# На HF Spaces используем HF Router/Api (Whisper).
# transformers
# torch
# librosa
# soundfile
# ffmpeg-python
